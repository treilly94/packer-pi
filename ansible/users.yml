---
- hosts: all
  become: true

  tasks:
    - name: Add sudo users
      user:
        name: "{{ item }}"
        shell: /bin/bash
        state: present
        groups: sudo
      loop:
        - tom
        - gen